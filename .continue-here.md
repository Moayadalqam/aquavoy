# 👋 Continue Here — Aquavoy

**For:** Moayad (or whoever picks this up next)
**Last worked:** 2026-07-02 by Moayad
**Branch:** `fix/gefo-transport-invoice` (pushed to fork). `main` = M6 shipped.
**State:** 🟢 **M7 "Full Office" OPEN** — M6 (Invoice Automation) closed 2026-07-02.
**⏰ Office meeting with Wency: Friday 2026-07-03** — brief at `.planning/meeting-2026-07-03-brief.md`.

---

## TL;DR — where we are

```
✓ M1–M5                    SHIPPED (see .planning/archive/)
✓ M6 Invoice Automation    SHIPPED 2026-06-29 → main · CLOSED 2026-07-02
→ M7 Full Office           OPEN — 7 phases from Wency's 2026-07-02 "perfect flow"
                           (scope: .planning/JOURNEY.md §M7 · roadmap: .planning/ROADMAP.md)
```

On 2026-07-02 Wency sent his full wish list + his REAL documents (26-001 Gefo
invoice, Gutschrift 26400013, Transportbestätigung, Reis registratie.xlsx).
Those exposed that the shipped `gefo.docx` had the wrong structure (crewing
instead of transportation) — **fixed same day** on `fix/gefo-transport-invoice`
(commit `d307a28`, tsc 0, 252 tests green, pushed).

## Do FIRST (before the Friday meeting)

1. **Ship `fix/gefo-transport-invoice` → main** (`/qualia-ship` / owner merge).
   Without it the demo generates the WRONG Gefo invoice format.
2. **Owner runs `vercel --prod`** — M6 is on main but the prod deploy was left
   as the OWNER's call (m6-prelaunch-checklist §2). Never verified live.
3. Revoke the leaked `sbp_…` Supabase token (open since M6).

## Demo warning

Do NOT tap Confirm on an **inbox-scan** card in front of the client — cron
stages finance/voyage proposals with empty company/amount and
`executeConfirmedAction` rejects them (shipped M6-P5 gap; M7 Phase 1 fixes it
via scan-time extraction). Drive the same flow from **chat** — that works
end-to-end: read Gutschrift → save attachment → generate invoice (confirm) →
OneDrive + push.

## Resume path

- After the meeting: fill the answers into `.planning/meeting-2026-07-03-brief.md`
  §3 (folder routing, numbering rules, BUNKERINGEN check, Revolut plan/CAMT
  sample, WhatsApp + phone sign-offs, Bargeplanner access, layover sample,
  style consent), then **`/qualia-plan 1`** (M7 P1: pipeline truth & accounting
  completeness — no client input needed to start).
- M7 arc + gates: `.planning/JOURNEY.md` §M7. Requirements: REQ-31+ in
  `.planning/REQUIREMENTS.md`.

## Watch out for

- **Register writes stay gated** until the BUNKERINGEN block position in the
  real `Reis registratie.xlsx` is verified (append targets "next empty row").
- **Invoice-number collision:** YY-NNN lives in Wency's head/OneDrive; confirm
  next free number + who assigns before enabling `nextInvoiceNumber`.
- **"Fully automated" expectation:** system is confirm-before-write (ADR-003)
  on purpose — per-action auto-execute rules are a trust conversation, not a default.
- Feature branches only; deploys are CLI-only (`vercel --prod`), auto-deploy off.

Full arc: `.planning/JOURNEY.md` · milestone archive `.planning/archive/` ·
glossary `.planning/CONTEXT.md` · ADRs `.planning/decisions/`.
